/// <reference types="cypress" />

describe.skip('Config and env challenge', () => {
  /* 
    Challenge #1: set up your base url and visit your app
  */ 
  it('opens app', () => {
    
  });

  /* 
    Challenge #2: change the app viewport inside cypress.json file
  */
  it('opens app in desktop mode', () => {
    
  });

  /* 
    Challenge #3: create your own cypress.env.json file and save some variables in it
    Run a test and access them via devtools
  */
  it('has env variables', () => {
    
  });

  /* 
    Challenge #4: add an env variable during your test
    Access devtools to see if they are stored
  */
  it('adds env variables', () => {
    
  });
});
